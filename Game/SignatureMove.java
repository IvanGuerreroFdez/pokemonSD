public class SignatureMove implements Strategy {
    public void action() { // Invokes the function to calculate and deal damage
        DamageCalculator damage = new DamageCalculator();
        damage.damageCalculator();
    }
}
